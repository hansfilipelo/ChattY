changelog from Room.

receiveMessage(message*)
sendMessage(message*) -> sendMessage(message*)
addRoom(Room&) -> addRoom(Room*)
map<string... > -> vector<message*>log
removeRoom
map<string,room*> recieved the name names.
